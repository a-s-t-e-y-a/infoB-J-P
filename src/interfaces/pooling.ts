export interface PoolingBoothInput {
    name: string;
    adhyakshaBooth: string;
    mundalId: number; // Assuming you want to connect to a Mundal using its ID
    sectorId: number; // Assuming you want to connect to a Sector using its ID
  }